/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

// Included inside `cuda_device::barrier` to keep existing paths stable.

/// Arrives at a CTA shared-memory barrier and returns its phase token.
/// Lowers to `mbarrier.arrive.shared.b64 <register>, [<address>];`.
///
/// # Safety
/// `bar` must point to a live, eight-byte-aligned `Barrier` in shared memory.
/// The barrier must be initialized, and this arrival must be included in the current phase's expected count.
/// Use the returned token only with this barrier and phase.
#[inline(never)]
pub unsafe fn mbarrier_arrive(bar: *const Barrier) -> u64 {
    let _ = bar;
    unreachable!("mbarrier_arrive called outside CUDA kernel context")
}

/// Arrives at a CTA shared-memory barrier by a dynamic count without completing the current phase and returns its prior opaque state.
/// Lowers to `mbarrier.arrive.noComplete.shared.b64 <register>, [<address>], <register>;`.
///
/// # Safety
/// `bar` must point to a live, eight-byte-aligned `Barrier` in shared memory.
/// The barrier must be initialized. `count` must be a valid PTX arrival count and this operation must not complete the current phase.
/// Use the returned opaque state only with this barrier and phase.
#[inline(never)]
pub unsafe fn mbarrier_arrive_no_complete(bar: *const Barrier, count: u32) -> u64 {
    let _ = (bar, count);
    unreachable!("mbarrier_arrive_no_complete called outside CUDA kernel context")
}

/// Initializes a CTA shared-memory barrier with the expected arrival count.
/// Lowers to `mbarrier.init.shared.b64 [<address>], <register-or-immediate>;`.
///
/// # Safety
/// `bar` must point to a live, eight-byte-aligned `Barrier` in shared memory.
/// Exactly one thread may initialize it. `expected_count` must be in `1..=0xFFFFF` and include every arrival in the phase.
/// The barrier must be uninitialized or invalidated, with no concurrent barrier operation.
#[inline(never)]
pub unsafe fn mbarrier_init(bar: *mut Barrier, expected_count: u32) {
    let _ = (bar, expected_count);
    unreachable!("mbarrier_init called outside CUDA kernel context")
}

/// Invalidates a CTA shared-memory barrier after its users have finished.
/// Lowers to `mbarrier.inval.shared.b64 [<address>];`.
///
/// # Safety
/// `bar` must point to a live, eight-byte-aligned `Barrier` in shared memory.
/// The barrier must be initialized and unused by all threads and asynchronous operations.
/// Exactly one thread may invalidate it.
#[inline(never)]
pub unsafe fn mbarrier_inval(bar: *mut Barrier) {
    let _ = bar;
    unreachable!("mbarrier_inval called outside CUDA kernel context")
}

/// Tests whether the CTA shared-memory barrier phase for a token is complete.
/// Lowers to `mbarrier.test_wait.shared.b64 <register>, [<address>], <register>;`.
///
/// # Safety
/// `bar` must point to a live, eight-byte-aligned `Barrier` in shared memory.
/// The barrier must be initialized. `token` must come from this barrier and phase.
#[inline(never)]
pub unsafe fn mbarrier_test_wait(bar: *const Barrier, token: u64) -> bool {
    let _ = (bar, token);
    unreachable!("mbarrier_test_wait called outside CUDA kernel context")
}
