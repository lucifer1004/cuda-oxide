/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

// Included inside `cuda_device::tma` to keep its public API stable.

/// Commits this thread's pending asynchronous bulk copies as one group.
#[inline(never)]
pub fn cp_async_bulk_commit_group() {
    unreachable!("cp_async_bulk_commit_group called outside CUDA kernel context")
}

/// Prefetches one tensor tile through a tensor map into L2.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_1d_l2(tensor_map: *const TmaDescriptor, coord0: i32) {
    let _ = (tensor_map, coord0);
    unreachable!("cp_async_bulk_prefetch_tensor_1d_l2 called outside CUDA kernel context")
}

/// Prefetches one tensor tile through a tensor map into L2 using an explicit cache hint.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_1d_l2_cache_hint(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    cache_hint: u64,
) {
    let _ = (tensor_map, coord0, cache_hint);
    unreachable!(
        "cp_async_bulk_prefetch_tensor_1d_l2_cache_hint called outside CUDA kernel context"
    )
}

/// Prefetches one tensor tile through a tensor map into L2.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_2d_l2(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_prefetch_tensor_2d_l2 called outside CUDA kernel context")
}

/// Prefetches one tensor tile through a tensor map into L2 using an explicit cache hint.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_2d_l2_cache_hint(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    cache_hint: u64,
) {
    let _ = (tensor_map, coord0, coord1, cache_hint);
    unreachable!(
        "cp_async_bulk_prefetch_tensor_2d_l2_cache_hint called outside CUDA kernel context"
    )
}

/// Prefetches one tensor tile through a tensor map into L2.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_3d_l2(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_prefetch_tensor_3d_l2 called outside CUDA kernel context")
}

/// Prefetches one tensor tile through a tensor map into L2 using an explicit cache hint.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_3d_l2_cache_hint(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    cache_hint: u64,
) {
    let _ = (tensor_map, coord0, coord1, coord2, cache_hint);
    unreachable!(
        "cp_async_bulk_prefetch_tensor_3d_l2_cache_hint called outside CUDA kernel context"
    )
}

/// Prefetches one tensor tile through a tensor map into L2.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_4d_l2(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_prefetch_tensor_4d_l2 called outside CUDA kernel context")
}

/// Prefetches one tensor tile through a tensor map into L2 using an explicit cache hint.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_4d_l2_cache_hint(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    cache_hint: u64,
) {
    let _ = (tensor_map, coord0, coord1, coord2, coord3, cache_hint);
    unreachable!(
        "cp_async_bulk_prefetch_tensor_4d_l2_cache_hint called outside CUDA kernel context"
    )
}

/// Prefetches one tensor tile through a tensor map into L2.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_5d_l2(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_prefetch_tensor_5d_l2 called outside CUDA kernel context")
}

/// Prefetches one tensor tile through a tensor map into L2 using an explicit cache hint.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_5d_l2_cache_hint(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
    cache_hint: u64,
) {
    let _ = (
        tensor_map, coord0, coord1, coord2, coord3, coord4, cache_hint,
    );
    unreachable!(
        "cp_async_bulk_prefetch_tensor_5d_l2_cache_hint called outside CUDA kernel context"
    )
}

/// Prefetches one tensor tile through a tensor map into L2.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_gather4_2d_l2(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_prefetch_tensor_gather4_2d_l2 called outside CUDA kernel context")
}

/// Prefetches one tensor tile through a tensor map into L2 using an explicit cache hint.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn cp_async_bulk_prefetch_tensor_gather4_2d_l2_cache_hint(
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
    cache_hint: u64,
) {
    let _ = (
        tensor_map, coord0, coord1, coord2, coord3, coord4, cache_hint,
    );
    unreachable!(
        "cp_async_bulk_prefetch_tensor_gather4_2d_l2_cache_hint called outside CUDA kernel context"
    )
}

/// Starts a TMA tile copy from global to cluster shared memory.
///
/// # Safety
/// `dst`, `tensor_map`, and `barrier` must remain valid until the copy completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_1d_g2s(
    dst: *mut u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    barrier: *mut Barrier,
) {
    let _ = (dst, tensor_map, coord0, barrier);
    unreachable!("cp_async_bulk_tensor_1d_g2s called outside CUDA kernel context")
}

/// Starts a TMA tile copy from shared to global memory.
///
/// # Safety
/// `src` and `tensor_map` must remain valid until the committed copy group completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_1d_s2g(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_1d_s2g called outside CUDA kernel context")
}

/// Starts a TMA tile copy from global to cluster shared memory.
///
/// # Safety
/// `dst`, `tensor_map`, and `barrier` must remain valid until the copy completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_2d_g2s(
    dst: *mut u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    barrier: *mut Barrier,
) {
    let _ = (dst, tensor_map, coord0, coord1, barrier);
    unreachable!("cp_async_bulk_tensor_2d_g2s called outside CUDA kernel context")
}

/// Starts a multicast TMA tile copy from global to cluster shared memory.
///
/// # Safety
/// `dst`, `tensor_map`, and `barrier` must remain valid until the copy completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_2d_g2s_multicast(
    dst: *mut u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    barrier: *mut Barrier,
    cta_mask: u16,
) {
    let _ = (dst, tensor_map, coord0, coord1, barrier, cta_mask);
    unreachable!("cp_async_bulk_tensor_2d_g2s_multicast called outside CUDA kernel context")
}

/// Starts a multicast TMA tile copy from global to cluster shared memory.
///
/// # Safety
/// `dst`, `tensor_map`, and `barrier` must remain valid until the copy completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_2d_g2s_multicast_cg2(
    dst: *mut u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    barrier: *mut Barrier,
    cta_mask: u16,
) {
    let _ = (dst, tensor_map, coord0, coord1, barrier, cta_mask);
    unreachable!("cp_async_bulk_tensor_2d_g2s_multicast_cg2 called outside CUDA kernel context")
}

/// Starts a TMA tile copy from shared to global memory.
///
/// # Safety
/// `src` and `tensor_map` must remain valid until the committed copy group completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_2d_s2g(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_2d_s2g called outside CUDA kernel context")
}

/// Starts a TMA tile copy from global to cluster shared memory.
///
/// # Safety
/// `dst`, `tensor_map`, and `barrier` must remain valid until the copy completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_3d_g2s(
    dst: *mut u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    barrier: *mut Barrier,
) {
    let _ = (dst, tensor_map, coord0, coord1, coord2, barrier);
    unreachable!("cp_async_bulk_tensor_3d_g2s called outside CUDA kernel context")
}

/// Starts a TMA tile copy from shared to global memory.
///
/// # Safety
/// `src` and `tensor_map` must remain valid until the committed copy group completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_3d_s2g(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_3d_s2g called outside CUDA kernel context")
}

/// Starts a TMA tile copy from global to cluster shared memory.
///
/// # Safety
/// `dst`, `tensor_map`, and `barrier` must remain valid until the copy completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_4d_g2s(
    dst: *mut u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    barrier: *mut Barrier,
) {
    let _ = (dst, tensor_map, coord0, coord1, coord2, coord3, barrier);
    unreachable!("cp_async_bulk_tensor_4d_g2s called outside CUDA kernel context")
}

/// Starts a TMA tile copy from shared to global memory.
///
/// # Safety
/// `src` and `tensor_map` must remain valid until the committed copy group completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_4d_s2g(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_4d_s2g called outside CUDA kernel context")
}

/// Starts a TMA tile copy from global to cluster shared memory.
///
/// # Safety
/// `dst`, `tensor_map`, and `barrier` must remain valid until the copy completes.
#[inline(never)]
#[allow(clippy::too_many_arguments)]
pub unsafe fn cp_async_bulk_tensor_5d_g2s(
    dst: *mut u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
    barrier: *mut Barrier,
) {
    let _ = (
        dst, tensor_map, coord0, coord1, coord2, coord3, coord4, barrier,
    );
    unreachable!("cp_async_bulk_tensor_5d_g2s called outside CUDA kernel context")
}

/// Starts a TMA tile copy from shared to global memory.
///
/// # Safety
/// `src` and `tensor_map` must remain valid until the committed copy group completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_5d_s2g(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_5d_s2g called outside CUDA kernel context")
}

/// Starts a TMA tensor add reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_add_im2col_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_add_im2col_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor add reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_add_im2col_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_add_im2col_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor add reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_add_im2col_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_add_im2col_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor add reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_add_tile_1d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_reduce_add_tile_1d called outside CUDA kernel context")
}

/// Starts a TMA tensor add reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_add_tile_2d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_reduce_add_tile_2d called outside CUDA kernel context")
}

/// Starts a TMA tensor add reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_add_tile_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_add_tile_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor add reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_add_tile_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_add_tile_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor add reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_add_tile_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_add_tile_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor and reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_and_im2col_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_and_im2col_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor and reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_and_im2col_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_and_im2col_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor and reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_and_im2col_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_and_im2col_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor and reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_and_tile_1d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_reduce_and_tile_1d called outside CUDA kernel context")
}

/// Starts a TMA tensor and reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_and_tile_2d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_reduce_and_tile_2d called outside CUDA kernel context")
}

/// Starts a TMA tensor and reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_and_tile_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_and_tile_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor and reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_and_tile_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_and_tile_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor and reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_and_tile_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_and_tile_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor dec reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_dec_im2col_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_dec_im2col_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor dec reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_dec_im2col_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_dec_im2col_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor dec reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_dec_im2col_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_dec_im2col_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor dec reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_dec_tile_1d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_reduce_dec_tile_1d called outside CUDA kernel context")
}

/// Starts a TMA tensor dec reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_dec_tile_2d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_reduce_dec_tile_2d called outside CUDA kernel context")
}

/// Starts a TMA tensor dec reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_dec_tile_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_dec_tile_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor dec reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_dec_tile_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_dec_tile_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor dec reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_dec_tile_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_dec_tile_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor inc reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_inc_im2col_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_inc_im2col_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor inc reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_inc_im2col_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_inc_im2col_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor inc reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_inc_im2col_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_inc_im2col_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor inc reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_inc_tile_1d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_reduce_inc_tile_1d called outside CUDA kernel context")
}

/// Starts a TMA tensor inc reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_inc_tile_2d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_reduce_inc_tile_2d called outside CUDA kernel context")
}

/// Starts a TMA tensor inc reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_inc_tile_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_inc_tile_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor inc reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_inc_tile_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_inc_tile_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor inc reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_inc_tile_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_inc_tile_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor max reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_max_im2col_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_max_im2col_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor max reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_max_im2col_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_max_im2col_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor max reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_max_im2col_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_max_im2col_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor max reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_max_tile_1d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_reduce_max_tile_1d called outside CUDA kernel context")
}

/// Starts a TMA tensor max reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_max_tile_2d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_reduce_max_tile_2d called outside CUDA kernel context")
}

/// Starts a TMA tensor max reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_max_tile_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_max_tile_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor max reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_max_tile_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_max_tile_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor max reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_max_tile_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_max_tile_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor min reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_min_im2col_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_min_im2col_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor min reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_min_im2col_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_min_im2col_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor min reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_min_im2col_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_min_im2col_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor min reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_min_tile_1d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_reduce_min_tile_1d called outside CUDA kernel context")
}

/// Starts a TMA tensor min reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_min_tile_2d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_reduce_min_tile_2d called outside CUDA kernel context")
}

/// Starts a TMA tensor min reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_min_tile_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_min_tile_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor min reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_min_tile_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_min_tile_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor min reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_min_tile_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_min_tile_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor or reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_or_im2col_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_or_im2col_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor or reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_or_im2col_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_or_im2col_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor or reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_or_im2col_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_or_im2col_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor or reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_or_tile_1d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_reduce_or_tile_1d called outside CUDA kernel context")
}

/// Starts a TMA tensor or reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_or_tile_2d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_reduce_or_tile_2d called outside CUDA kernel context")
}

/// Starts a TMA tensor or reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_or_tile_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_or_tile_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor or reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_or_tile_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_or_tile_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor or reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_or_tile_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_or_tile_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor xor reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_xor_im2col_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_xor_im2col_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor xor reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_xor_im2col_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_xor_im2col_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor xor reduction from shared to global memory in im2col mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_xor_im2col_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_xor_im2col_5d called outside CUDA kernel context")
}

/// Starts a TMA tensor xor reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_xor_tile_1d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
) {
    let _ = (src, tensor_map, coord0);
    unreachable!("cp_async_bulk_tensor_reduce_xor_tile_1d called outside CUDA kernel context")
}

/// Starts a TMA tensor xor reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_xor_tile_2d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
) {
    let _ = (src, tensor_map, coord0, coord1);
    unreachable!("cp_async_bulk_tensor_reduce_xor_tile_2d called outside CUDA kernel context")
}

/// Starts a TMA tensor xor reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_xor_tile_3d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2);
    unreachable!("cp_async_bulk_tensor_reduce_xor_tile_3d called outside CUDA kernel context")
}

/// Starts a TMA tensor xor reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_xor_tile_4d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3);
    unreachable!("cp_async_bulk_tensor_reduce_xor_tile_4d called outside CUDA kernel context")
}

/// Starts a TMA tensor xor reduction from shared to global memory in tile mode.
///
/// # Safety
/// `src` must name a live shared-memory tile and `tensor_map` must describe a compatible global destination until the committed reduction completes.
#[inline(never)]
pub unsafe fn cp_async_bulk_tensor_reduce_xor_tile_5d(
    src: *const u8,
    tensor_map: *const TmaDescriptor,
    coord0: i32,
    coord1: i32,
    coord2: i32,
    coord3: i32,
    coord4: i32,
) {
    let _ = (src, tensor_map, coord0, coord1, coord2, coord3, coord4);
    unreachable!("cp_async_bulk_tensor_reduce_xor_tile_5d called outside CUDA kernel context")
}

/// Waits until at most the requested bulk-copy groups remain pending.
#[inline(never)]
pub fn cp_async_bulk_wait_group(n: u32) {
    let _ = n;
    unreachable!("cp_async_bulk_wait_group called outside CUDA kernel context")
}

/// Waits for bulk-copy groups and completes their reads.
#[inline(never)]
pub fn cp_async_bulk_wait_group_read(n: u32) {
    let _ = n;
    unreachable!("cp_async_bulk_wait_group_read called outside CUDA kernel context")
}

/// Acquires a published tensor-map descriptor into the selected tensor-map proxy scope.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn fence_proxy_tensormap_generic_acquire_cluster(tensor_map: *const TmaDescriptor) {
    let _ = tensor_map;
    unreachable!("fence_proxy_tensormap_generic_acquire_cluster called outside CUDA kernel context")
}

/// Acquires a published tensor-map descriptor into the selected tensor-map proxy scope.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn fence_proxy_tensormap_generic_acquire_cta(tensor_map: *const TmaDescriptor) {
    let _ = tensor_map;
    unreachable!("fence_proxy_tensormap_generic_acquire_cta called outside CUDA kernel context")
}

/// Acquires a published tensor-map descriptor into the selected tensor-map proxy scope.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn fence_proxy_tensormap_generic_acquire_gpu(tensor_map: *const TmaDescriptor) {
    let _ = tensor_map;
    unreachable!("fence_proxy_tensormap_generic_acquire_gpu called outside CUDA kernel context")
}

/// Acquires a published tensor-map descriptor into the selected tensor-map proxy scope.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn fence_proxy_tensormap_generic_acquire_system(tensor_map: *const TmaDescriptor) {
    let _ = tensor_map;
    unreachable!("fence_proxy_tensormap_generic_acquire_system called outside CUDA kernel context")
}

/// Publishes prior generic-proxy tensor-map writes to the selected tensor-map proxy scope.
#[inline(never)]
pub fn fence_proxy_tensormap_generic_release_cluster() {
    unreachable!("fence_proxy_tensormap_generic_release_cluster called outside CUDA kernel context")
}

/// Publishes prior generic-proxy tensor-map writes to the selected tensor-map proxy scope.
#[inline(never)]
pub fn fence_proxy_tensormap_generic_release_cta() {
    unreachable!("fence_proxy_tensormap_generic_release_cta called outside CUDA kernel context")
}

/// Publishes prior generic-proxy tensor-map writes to the selected tensor-map proxy scope.
#[inline(never)]
pub fn fence_proxy_tensormap_generic_release_gpu() {
    unreachable!("fence_proxy_tensormap_generic_release_gpu called outside CUDA kernel context")
}

/// Publishes prior generic-proxy tensor-map writes to the selected tensor-map proxy scope.
#[inline(never)]
pub fn fence_proxy_tensormap_generic_release_system() {
    unreachable!("fence_proxy_tensormap_generic_release_system called outside CUDA kernel context")
}

/// Prefetches a live tensor-map descriptor into the tensor-map cache.
///
/// # Safety
/// `tensor_map` must point to a live tensor-map descriptor.
#[inline(never)]
pub unsafe fn prefetch_tma_descriptor(tensor_map: *const TmaDescriptor) {
    let _ = tensor_map;
    unreachable!("prefetch_tma_descriptor called outside CUDA kernel context")
}

/// Replaces one indexed 32-bit field in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_box_dim(
    tensor_map: *mut TmaDescriptor,
    ordinal: u32,
    new_value: u32,
) {
    let _ = (tensor_map, ordinal, new_value);
    unreachable!("tensormap_replace_box_dim called outside CUDA kernel context")
}

/// Replaces one indexed 32-bit field in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_element_stride(
    tensor_map: *mut TmaDescriptor,
    ordinal: u32,
    new_value: u32,
) {
    let _ = (tensor_map, ordinal, new_value);
    unreachable!("tensormap_replace_element_stride called outside CUDA kernel context")
}

/// Replaces one immediate-valued field in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_element_type(tensor_map: *mut TmaDescriptor, new_value: u32) {
    let _ = (tensor_map, new_value);
    unreachable!("tensormap_replace_element_type called outside CUDA kernel context")
}

/// Replaces one immediate-valued field in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_fill_mode(tensor_map: *mut TmaDescriptor, new_value: u32) {
    let _ = (tensor_map, new_value);
    unreachable!("tensormap_replace_fill_mode called outside CUDA kernel context")
}

/// Replaces the global base address stored in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_global_address(
    tensor_map: *mut TmaDescriptor,
    new_address: *const u8,
) {
    let _ = (tensor_map, new_address);
    unreachable!("tensormap_replace_global_address called outside CUDA kernel context")
}

/// Replaces one indexed 32-bit field in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_global_dim(
    tensor_map: *mut TmaDescriptor,
    ordinal: u32,
    new_value: u32,
) {
    let _ = (tensor_map, ordinal, new_value);
    unreachable!("tensormap_replace_global_dim called outside CUDA kernel context")
}

/// Replaces one indexed 64-bit stride in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_global_stride(
    tensor_map: *mut TmaDescriptor,
    ordinal: u32,
    new_value: u64,
) {
    let _ = (tensor_map, ordinal, new_value);
    unreachable!("tensormap_replace_global_stride called outside CUDA kernel context")
}

/// Replaces one immediate-valued field in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_interleave_layout(tensor_map: *mut TmaDescriptor, new_value: u32) {
    let _ = (tensor_map, new_value);
    unreachable!("tensormap_replace_interleave_layout called outside CUDA kernel context")
}

/// Replaces the rank stored in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_rank(tensor_map: *mut TmaDescriptor, new_value: u32) {
    let _ = (tensor_map, new_value);
    unreachable!("tensormap_replace_rank called outside CUDA kernel context")
}

/// Replaces one immediate-valued field in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_swizzle_atomicity(tensor_map: *mut TmaDescriptor, new_value: u32) {
    let _ = (tensor_map, new_value);
    unreachable!("tensormap_replace_swizzle_atomicity called outside CUDA kernel context")
}

/// Replaces one immediate-valued field in a writable tensor-map descriptor.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in global memory.
#[inline(never)]
pub unsafe fn tensormap_replace_swizzle_mode(tensor_map: *mut TmaDescriptor, new_value: u32) {
    let _ = (tensor_map, new_value);
    unreachable!("tensormap_replace_swizzle_mode called outside CUDA kernel context")
}

/// Replace a global tensor-map address and publish it to the tensor-map proxy.
///
/// # Safety
/// `tensor_map` must point to a writable, 128-byte tensor-map descriptor in
/// global memory, and `new_address` must remain valid for every later TMA use.
#[inline(always)]
pub unsafe fn replace_tma_global_address(tensor_map: *mut TmaDescriptor, new_address: *const u8) {
    unsafe { tensormap_replace_global_address(tensor_map, new_address) };
    fence_proxy_tensormap_generic_release_gpu();
    unsafe { fence_proxy_tensormap_generic_acquire_gpu(tensor_map) };
}
