/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `extended_minmax` intrinsics.

use super::helpers::require_arity;
use crate::error::TranslationResult;
use crate::translator::{rvalue, terminator::helpers, values::ValueMap};
use dialect_nvvm::ops::{
    ExtendedMinMaxFormatAttr, ExtendedMinMaxNanAttr, ExtendedMinMaxOp, ExtendedMinMaxOperationAttr,
    ExtendedMinMaxSubnormalAttr, ExtendedMinMaxXorSignAbsAttr,
};
use pliron::basic_block::BasicBlock;
use pliron::context::{Context, Ptr};
use pliron::location::{Located, Location};
use pliron::operation::Operation;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0850" | "cuda_device::bf16::max_bf16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0850");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_bf16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0842" | "cuda_device::f16::max_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0842");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0843"
        | "cuda_device::f16::max_ftz_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0843");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0564"
        | "cuda_device::f16x2::max_ftz_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0564");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0845"
        | "cuda_device::f16::max_ftz_nan_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0845");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_nan_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0565"
        | "cuda_device::f16x2::max_ftz_nan_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0565");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_nan_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0849"
        | "cuda_device::f16::max_ftz_nan_xorsign_abs_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0849");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_nan_xorsign_abs_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0567"
        | "cuda_device::f16x2::max_ftz_nan_xorsign_abs_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0567");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_nan_xorsign_abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0566"
        | "cuda_device::float::max_ftz_nan_xorsign_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F32,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0566");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_nan_xorsign_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0847"
        | "cuda_device::f16::max_ftz_xorsign_abs_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0847");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_xorsign_abs_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0569"
        | "cuda_device::f16x2::max_ftz_xorsign_abs_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0569");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_xorsign_abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0568"
        | "cuda_device::float::max_ftz_xorsign_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F32,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0568");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_ftz_xorsign_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0851"
        | "cuda_device::bf16::max_nan_bf16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0851");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_bf16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0570"
        | "cuda_device::bf16x2::max_nan_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0570");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0844"
        | "cuda_device::f16::max_nan_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0844");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0571"
        | "cuda_device::f16x2::max_nan_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0571");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0853"
        | "cuda_device::bf16::max_nan_xorsign_abs_bf16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0853");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_xorsign_abs_bf16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0572"
        | "cuda_device::bf16x2::max_nan_xorsign_abs_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0572");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_xorsign_abs_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0848"
        | "cuda_device::f16::max_nan_xorsign_abs_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0848");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_xorsign_abs_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0574"
        | "cuda_device::f16x2::max_nan_xorsign_abs_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0574");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_xorsign_abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0573"
        | "cuda_device::float::max_nan_xorsign_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F32,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0573");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_nan_xorsign_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0852"
        | "cuda_device::bf16::max_xorsign_abs_bf16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0852");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_xorsign_abs_bf16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0575"
        | "cuda_device::bf16x2::max_xorsign_abs_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0575");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_xorsign_abs_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0846"
        | "cuda_device::f16::max_xorsign_abs_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0846");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_xorsign_abs_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0577"
        | "cuda_device::f16x2::max_xorsign_abs_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0577");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_xorsign_abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0576"
        | "cuda_device::float::max_xorsign_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F32,
                ExtendedMinMaxOperationAttr::Max,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0576");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_xorsign_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0838" | "cuda_device::bf16::min_bf16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0838");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_bf16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0830" | "cuda_device::f16::min_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0830");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0831"
        | "cuda_device::f16::min_ftz_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0831");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0550"
        | "cuda_device::f16x2::min_ftz_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0550");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0833"
        | "cuda_device::f16::min_ftz_nan_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0833");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_nan_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0551"
        | "cuda_device::f16x2::min_ftz_nan_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0551");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_nan_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0837"
        | "cuda_device::f16::min_ftz_nan_xorsign_abs_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0837");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_nan_xorsign_abs_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0553"
        | "cuda_device::f16x2::min_ftz_nan_xorsign_abs_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0553");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_nan_xorsign_abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0552"
        | "cuda_device::float::min_ftz_nan_xorsign_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F32,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0552");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_nan_xorsign_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0835"
        | "cuda_device::f16::min_ftz_xorsign_abs_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0835");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_xorsign_abs_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0555"
        | "cuda_device::f16x2::min_ftz_xorsign_abs_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0555");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_xorsign_abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0554"
        | "cuda_device::float::min_ftz_xorsign_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F32,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Ftz,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0554");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_ftz_xorsign_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0839"
        | "cuda_device::bf16::min_nan_bf16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0839");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_bf16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0556"
        | "cuda_device::bf16x2::min_nan_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0556");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0832"
        | "cuda_device::f16::min_nan_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0832");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0557"
        | "cuda_device::f16x2::min_nan_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Disabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0557");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0841"
        | "cuda_device::bf16::min_nan_xorsign_abs_bf16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0841");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_xorsign_abs_bf16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0558"
        | "cuda_device::bf16x2::min_nan_xorsign_abs_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0558");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_xorsign_abs_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0836"
        | "cuda_device::f16::min_nan_xorsign_abs_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0836");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_xorsign_abs_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0560"
        | "cuda_device::f16x2::min_nan_xorsign_abs_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0560");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_xorsign_abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0559"
        | "cuda_device::float::min_nan_xorsign_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F32,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Nan,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0559");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_nan_xorsign_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0840"
        | "cuda_device::bf16::min_xorsign_abs_bf16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0840");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_xorsign_abs_bf16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0561"
        | "cuda_device::bf16x2::min_xorsign_abs_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::Bf16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0561");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_xorsign_abs_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0834"
        | "cuda_device::f16::min_xorsign_abs_f16" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0834");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_xorsign_abs_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0563"
        | "cuda_device::f16x2::min_xorsign_abs_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F16x2,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0563");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_xorsign_abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0562"
        | "cuda_device::float::min_xorsign_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (a, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (b, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = ExtendedMinMaxOp::build(
                ctx,
                a,
                b,
                ExtendedMinMaxFormatAttr::F32,
                ExtendedMinMaxOperationAttr::Min,
                ExtendedMinMaxSubnormalAttr::Preserve,
                ExtendedMinMaxNanAttr::Number,
                ExtendedMinMaxXorSignAbsAttr::Enabled,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0562");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_xorsign_abs_f32 call without target block",
            )?))
        }
        _ => Ok(None),
    }
}
