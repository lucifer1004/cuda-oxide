/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `integer_minmax` intrinsics.

use super::helpers::require_arity;
use crate::error::TranslationResult;
use crate::translator::{rvalue, terminator::helpers, values::ValueMap};
use dialect_nvvm::ops::{
    MaxReluS16x2Op, MaxReluS32Op, MaxS16x2Op, MaxU16x2Op, MinReluS16x2Op, MinReluS32Op, MinS16x2Op,
    MinU16x2Op,
};
use pliron::basic_block::BasicBlock;
use pliron::context::{Context, Ptr};
use pliron::location::{Located, Location};
use pliron::operation::Operation;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0994"
        | "cuda_device::i16x2::max_relu_s16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MaxReluS16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0994");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_relu_s16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0988"
        | "cuda_device::int::max_relu_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MaxReluS32Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0988");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_relu_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0990"
        | "cuda_device::i16x2::max_s16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MaxS16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0990");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_s16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0992"
        | "cuda_device::i16x2::max_u16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MaxU16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0992");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_u16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0993"
        | "cuda_device::i16x2::min_relu_s16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MinReluS16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0993");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_relu_s16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0987"
        | "cuda_device::int::min_relu_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MinReluS32Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0987");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_relu_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0989"
        | "cuda_device::i16x2::min_s16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MinS16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0989");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_s16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0991"
        | "cuda_device::i16x2::min_u16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MinU16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0991");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_u16x2 call without target block",
            )?))
        }
        _ => Ok(None),
    }
}
