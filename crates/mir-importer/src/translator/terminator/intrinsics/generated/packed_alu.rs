/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `packed_alu` intrinsics.

use super::helpers::require_arity;
use crate::error::TranslationResult;
use crate::translator::{rvalue, terminator::helpers, values::ValueMap};
use dialect_nvvm::ops::{
    AbsBf16x2Op, AbsF16x2Op, AddBf16x2Op, AddF16x2Op, AddF32x2Op, AddFtzF32x2Op, FmaBf16x2Op,
    FmaF16x2Op, FmaF32x2Op, FmaFtzF16x2Op, FmaFtzF32x2Op, FmaFtzReluF16x2Op, FmaFtzSatF16x2Op,
    FmaReluBf16x2Op, FmaReluF16x2Op, FmaSatF16x2Op, MaxBf16x2Op, MaxF16x2Op, MinBf16x2Op,
    MinF16x2Op, MulBf16x2Op, MulF16x2Op, MulF32x2Op, MulFtzF32x2Op, NegBf16x2Op, NegF16x2Op,
    SubBf16x2Op, SubF16x2Op, SubF32x2Op, SubFtzF32x2Op,
};
use pliron::basic_block::BasicBlock;
use pliron::context::{Context, Ptr};
use pliron::location::{Located, Location};
use pliron::operation::Operation;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0070"
        | "cuda_device::bf16x2::abs_bf16x2" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = AbsBf16x2Op::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0070");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "abs_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0080"
        | "cuda_device::f16x2::abs_f16x2" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = AbsF16x2Op::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0080");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "abs_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0064"
        | "cuda_device::bf16x2::add_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = AddBf16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0064");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "add_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0074"
        | "cuda_device::f16x2::add_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = AddF16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0074");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "add_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0995"
        | "cuda_device::f32x2::add_f32x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = AddF32x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0995");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "add_f32x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0996"
        | "cuda_device::f32x2::add_ftz_f32x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = AddFtzF32x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0996");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "add_ftz_f32x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0062"
        | "cuda_device::bf16x2::fma_bf16x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaBf16x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0062");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0072"
        | "cuda_device::f16x2::fma_f16x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaF16x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0072");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1001"
        | "cuda_device::f32x2::fma_f32x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaF32x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1001");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_f32x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0854"
        | "cuda_device::f16x2::fma_ftz_f16x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaFtzF16x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0854");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_ftz_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1002"
        | "cuda_device::f32x2::fma_ftz_f32x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaFtzF32x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1002");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_ftz_f32x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0857"
        | "cuda_device::f16x2::fma_ftz_relu_f16x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaFtzReluF16x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0857");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_ftz_relu_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0856"
        | "cuda_device::f16x2::fma_ftz_sat_f16x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaFtzSatF16x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0856");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_ftz_sat_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0063"
        | "cuda_device::bf16x2::fma_relu_bf16x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaReluBf16x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0063");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_relu_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0073"
        | "cuda_device::f16x2::fma_relu_f16x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaReluF16x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0073");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_relu_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0855"
        | "cuda_device::f16x2::fma_sat_f16x2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let (arg2, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[2],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = FmaSatF16x2Op::build(ctx, arg0, arg1, arg2);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0855");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "fma_sat_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0068"
        | "cuda_device::bf16x2::max_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MaxBf16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0068");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0078"
        | "cuda_device::f16x2::max_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MaxF16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0078");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "max_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0067"
        | "cuda_device::bf16x2::min_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MinBf16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0067");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0077"
        | "cuda_device::f16x2::min_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MinF16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0077");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "min_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0066"
        | "cuda_device::bf16x2::mul_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MulBf16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0066");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "mul_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0076"
        | "cuda_device::f16x2::mul_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MulF16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0076");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "mul_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0999"
        | "cuda_device::f32x2::mul_f32x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MulF32x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0999");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "mul_f32x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1000"
        | "cuda_device::f32x2::mul_ftz_f32x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = MulFtzF32x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1000");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "mul_ftz_f32x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0069"
        | "cuda_device::bf16x2::neg_bf16x2" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = NegBf16x2Op::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0069");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "neg_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0079"
        | "cuda_device::f16x2::neg_f16x2" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = NegF16x2Op::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0079");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "neg_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0065"
        | "cuda_device::bf16x2::sub_bf16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = SubBf16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0065");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sub_bf16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0075"
        | "cuda_device::f16x2::sub_f16x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = SubF16x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0075");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sub_f16x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0997"
        | "cuda_device::f32x2::sub_f32x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = SubF32x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0997");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sub_f32x2 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0998"
        | "cuda_device::f32x2::sub_ftz_f32x2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (arg1, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let intrinsic = SubFtzF32x2Op::build(ctx, arg0, arg1);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0998");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sub_ftz_f32x2 call without target block",
            )?))
        }
        _ => Ok(None),
    }
}
