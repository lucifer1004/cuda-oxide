/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `redux` intrinsics.

use super::helpers::require_arity;
use crate::error::TranslationResult;
use crate::translator::{rvalue, terminator::helpers, values::ValueMap};
use dialect_nvvm::ops::{
    ReduxSyncAddOp, ReduxSyncAndOp, ReduxSyncFmaxAbsNanOp, ReduxSyncFmaxAbsOp, ReduxSyncFmaxNanOp,
    ReduxSyncFmaxOp, ReduxSyncFminAbsNanOp, ReduxSyncFminAbsOp, ReduxSyncFminNanOp,
    ReduxSyncFminOp, ReduxSyncMaxOp, ReduxSyncMinOp, ReduxSyncOrOp, ReduxSyncUmaxOp,
    ReduxSyncUminOp, ReduxSyncXorOp,
};
use pliron::basic_block::BasicBlock;
use pliron::context::{Context, Ptr};
use pliron::location::{Located, Location};
use pliron::operation::Operation;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0017"
        | "cuda_device::warp::redux_sync_add" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncAddOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i0017");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_add call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0022"
        | "cuda_device::warp::redux_sync_and" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncAndOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i0022");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_and call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1009"
        | "cuda_device::warp::redux_sync_max_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncFmaxAbsOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i1009");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1010"
        | "cuda_device::warp::redux_sync_max_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncFmaxAbsNanOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i1010");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1007"
        | "cuda_device::warp::redux_sync_max_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncFmaxOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i1007");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0021"
        | "cuda_device::warp::redux_sync_max_i32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncMaxOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i0021");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_max_i32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1008"
        | "cuda_device::warp::redux_sync_max_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncFmaxNanOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i1008");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0020"
        | "cuda_device::warp::redux_sync_max_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncUmaxOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i0020");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1005"
        | "cuda_device::warp::redux_sync_min_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncFminAbsOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i1005");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1006"
        | "cuda_device::warp::redux_sync_min_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncFminAbsNanOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i1006");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1003"
        | "cuda_device::warp::redux_sync_min_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncFminOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i1003");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0019"
        | "cuda_device::warp::redux_sync_min_i32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncMinOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i0019");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_min_i32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1004"
        | "cuda_device::warp::redux_sync_min_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncFminNanOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i1004");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0018"
        | "cuda_device::warp::redux_sync_min_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncUminOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i0018");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0023"
        | "cuda_device::warp::redux_sync_or" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncOrOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i0023");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_or call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0024"
        | "cuda_device::warp::redux_sync_xor" => {
            require_arity(name, args.len(), 2, &loc)?;
            let (member_mask, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let (value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[1],
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            let reduction = ReduxSyncXorOp::build(ctx, member_mask, value);
            reduction.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, reduction, "v1:i0024");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, reduction, block_ptr, prepared_last_op);
            let result = reduction.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                reduction,
                value_map,
                block_map,
                loc,
                "redux_sync_xor call without target block",
            )?))
        }
        _ => Ok(None),
    }
}
