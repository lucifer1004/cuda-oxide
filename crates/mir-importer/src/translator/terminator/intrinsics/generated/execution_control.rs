/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `execution_control` intrinsics.

use super::helpers::require_arity;
use crate::error::{TranslationErr, TranslationResult};
use crate::translator::{rvalue, terminator::helpers, values::ValueMap};
use dialect_mir::ops::MirConstantOp;
use dialect_nvvm::ops::{
    BarrierCtaArriveAlignedCountOp, BarrierCtaArriveCountOp, BarrierCtaSyncAlignedCountOp,
    BarrierCtaSyncCountOp, GridDependencyLaunchDependentsOp, GridDependencyWaitOp, SetMaxNRegDecOp,
    SetMaxNRegIncOp,
};
use pliron::basic_block::BasicBlock;
use pliron::context::{Context, Ptr};
use pliron::input_err;
use pliron::location::{Located, Location};
use pliron::op::Op;
use pliron::operation::Operation;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    _destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0885"
        | "cuda_device::barrier::barrier_cta_arrive" => {
            require_arity(name, args.len(), 2, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let control = Operation::new(
                ctx,
                BarrierCtaArriveCountOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            control.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, control, "v1:i0885");
            helpers::insert_op(ctx, control, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    control,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "barrier_cta_arrive call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0886"
        | "cuda_device::barrier::barrier_cta_arrive_aligned" => {
            require_arity(name, args.len(), 2, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let control = Operation::new(
                ctx,
                BarrierCtaArriveAlignedCountOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            control.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, control, "v1:i0886");
            helpers::insert_op(ctx, control, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    control,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "barrier_cta_arrive_aligned call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0883"
        | "cuda_device::barrier::barrier_cta_sync" => {
            require_arity(name, args.len(), 2, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let control = Operation::new(
                ctx,
                BarrierCtaSyncCountOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            control.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, control, "v1:i0883");
            helpers::insert_op(ctx, control, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    control,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "barrier_cta_sync call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0884"
        | "cuda_device::barrier::barrier_cta_sync_aligned" => {
            require_arity(name, args.len(), 2, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let control = Operation::new(
                ctx,
                BarrierCtaSyncAlignedCountOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            control.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, control, "v1:i0884");
            helpers::insert_op(ctx, control, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    control,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "barrier_cta_sync_aligned call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0913"
        | "cuda_device::grid::dependency::trigger_dependents" => {
            require_arity(name, args.len(), 0, &loc)?;
            let last_op = prev_op;
            let control = Operation::new(
                ctx,
                GridDependencyLaunchDependentsOp::get_concrete_op_info(),
                vec![],
                vec![],
                vec![],
                0,
            );
            control.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, control, "v1:i0913");
            helpers::insert_op(ctx, control, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    control,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "grid_dependency_launch_dependents call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0914"
        | "cuda_device::grid::dependency::wait" => {
            require_arity(name, args.len(), 0, &loc)?;
            let last_op = prev_op;
            let control = Operation::new(
                ctx,
                GridDependencyWaitOp::get_concrete_op_info(),
                vec![],
                vec![],
                vec![],
                0,
            );
            control.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, control, "v1:i0914");
            helpers::insert_op(ctx, control, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    control,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "grid_dependency_wait call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0916"
        | "cuda_device::thread::__setmaxnreg_dec" => {
            require_arity(name, args.len(), 1, &loc)?;
            if !matches!(&args[0], mir::Operand::Constant(_)) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "setmaxnreg requires a compile-time register count in 24..=256 divisible by 8".to_owned()
                    )
                );
            }
            let (register_count_value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let register_count = register_count_value
                .defining_op()
                .and_then(|defining_op| Operation::get_op::<MirConstantOp>(defining_op, ctx))
                .and_then(|constant| constant.get_attr_value(ctx))
                .map(|value| value.value().to_u64())
                .and_then(|value| u32::try_from(value).ok())
                .filter(|value| (24..=256).contains(value) && value % 8 == 0);
            let Some(register_count) = register_count else {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "setmaxnreg requires a compile-time register count in 24..=256 divisible by 8".to_owned()
                    )
                );
            };
            let control = SetMaxNRegDecOp::build(ctx, register_count);
            control.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, control, "v1:i0916");
            helpers::insert_op(ctx, control, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    control,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "setmaxnreg_dec call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0915"
        | "cuda_device::thread::__setmaxnreg_inc" => {
            require_arity(name, args.len(), 1, &loc)?;
            if !matches!(&args[0], mir::Operand::Constant(_)) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "setmaxnreg requires a compile-time register count in 24..=256 divisible by 8".to_owned()
                    )
                );
            }
            let (register_count_value, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let register_count = register_count_value
                .defining_op()
                .and_then(|defining_op| Operation::get_op::<MirConstantOp>(defining_op, ctx))
                .and_then(|constant| constant.get_attr_value(ctx))
                .map(|value| value.value().to_u64())
                .and_then(|value| u32::try_from(value).ok())
                .filter(|value| (24..=256).contains(value) && value % 8 == 0);
            let Some(register_count) = register_count else {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "setmaxnreg requires a compile-time register count in 24..=256 divisible by 8".to_owned()
                    )
                );
            };
            let control = SetMaxNRegIncOp::build(ctx, register_count);
            control.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, control, "v1:i0915");
            helpers::insert_op(ctx, control, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    control,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "setmaxnreg_inc call without target block".to_owned()
                    )
                )
            }
        }
        _ => Ok(None),
    }
}
