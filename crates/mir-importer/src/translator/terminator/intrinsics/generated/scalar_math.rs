/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `scalar_math` intrinsics.

use super::helpers::require_arity;
use crate::error::TranslationResult;
use crate::translator::{rvalue, terminator::helpers, values::ValueMap};
use dialect_nvvm::ops::{
    ScalarMathFormatAttr, ScalarMathOp, ScalarMathOperationAttr, ScalarMathPrecisionAttr,
    ScalarMathSubnormalAttr,
};
use pliron::basic_block::BasicBlock;
use pliron::context::{Context, Ptr};
use pliron::location::{Located, Location};
use pliron::operation::Operation;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0784"
        | "cuda_device::float::cos_approx_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Cos,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0784");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "cos_approx_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0785"
        | "cuda_device::float::cos_approx_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Cos,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0785");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "cos_approx_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1016"
        | "cuda_device::float::ex2_approx_f16" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F16,
                ScalarMathOperationAttr::Ex2,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1016");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "ex2_approx_f16 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0819"
        | "cuda_device::float::ex2_approx_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Ex2,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0819");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "ex2_approx_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0820"
        | "cuda_device::float::ex2_approx_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Ex2,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0820");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "ex2_approx_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0786"
        | "cuda_device::float::lg2_approx_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Lg2,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0786");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "lg2_approx_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0787"
        | "cuda_device::float::lg2_approx_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Lg2,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0787");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "lg2_approx_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0788"
        | "cuda_device::float::rcp_approx_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0788");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_approx_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0797"
        | "cuda_device::float::rcp_approx_ftz_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0797");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_approx_ftz_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0793"
        | "cuda_device::float::rcp_rm_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rm,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0793");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rm_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0800"
        | "cuda_device::float::rcp_rm_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rm,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0800");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rm_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0794"
        | "cuda_device::float::rcp_rm_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rm,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0794");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rm_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0789"
        | "cuda_device::float::rcp_rn_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rn,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0789");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rn_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0798"
        | "cuda_device::float::rcp_rn_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rn,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0798");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rn_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0790"
        | "cuda_device::float::rcp_rn_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rn,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0790");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rn_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0795"
        | "cuda_device::float::rcp_rp_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rp,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0795");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rp_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0801"
        | "cuda_device::float::rcp_rp_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rp,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0801");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rp_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0796"
        | "cuda_device::float::rcp_rp_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rp,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0796");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rp_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0791"
        | "cuda_device::float::rcp_rz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rz,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0791");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0799"
        | "cuda_device::float::rcp_rz_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rz,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0799");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rz_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0792"
        | "cuda_device::float::rcp_rz_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rcp,
                ScalarMathPrecisionAttr::Rz,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0792");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rcp_rz_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0802"
        | "cuda_device::float::rsqrt_approx_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rsqrt,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0802");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rsqrt_approx_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0804"
        | "cuda_device::float::rsqrt_approx_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Rsqrt,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0804");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rsqrt_approx_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0803"
        | "cuda_device::float::rsqrt_approx_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Rsqrt,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0803");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "rsqrt_approx_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0782"
        | "cuda_device::float::sin_approx_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sin,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0782");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sin_approx_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0783"
        | "cuda_device::float::sin_approx_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sin,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0783");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sin_approx_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0805"
        | "cuda_device::float::sqrt_approx_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0805");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_approx_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0806"
        | "cuda_device::float::sqrt_approx_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0806");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_approx_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0811"
        | "cuda_device::float::sqrt_rm_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rm,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0811");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rm_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0817"
        | "cuda_device::float::sqrt_rm_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rm,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0817");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rm_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0812"
        | "cuda_device::float::sqrt_rm_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rm,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0812");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rm_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0807"
        | "cuda_device::float::sqrt_rn_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rn,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0807");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rn_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0815"
        | "cuda_device::float::sqrt_rn_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rn,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0815");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rn_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0808"
        | "cuda_device::float::sqrt_rn_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rn,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0808");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rn_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0813"
        | "cuda_device::float::sqrt_rp_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rp,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0813");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rp_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0818"
        | "cuda_device::float::sqrt_rp_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rp,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0818");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rp_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0814"
        | "cuda_device::float::sqrt_rp_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rp,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0814");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rp_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0809"
        | "cuda_device::float::sqrt_rz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rz,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0809");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0816"
        | "cuda_device::float::sqrt_rz_f64" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F64,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rz,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0816");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rz_f64 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0810"
        | "cuda_device::float::sqrt_rz_ftz_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Sqrt,
                ScalarMathPrecisionAttr::Rz,
                ScalarMathSubnormalAttr::Ftz,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0810");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "sqrt_rz_ftz_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0821"
        | "cuda_device::float::tanh_approx_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = ScalarMathOp::build(
                ctx,
                arg0,
                ScalarMathFormatAttr::F32,
                ScalarMathOperationAttr::Tanh,
                ScalarMathPrecisionAttr::Approx,
                ScalarMathSubnormalAttr::Preserve,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0821");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "tanh_approx_f32 call without target block",
            )?))
        }
        _ => Ok(None),
    }
}
