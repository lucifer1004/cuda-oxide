/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `tma` intrinsics.

use super::helpers::require_arity;
use crate::error::{TranslationErr, TranslationResult};
use crate::translator::{rvalue, terminator::helpers, values::ValueMap};
use pliron::basic_block::BasicBlock;
use pliron::context::{Context, Ptr};
use pliron::input_err;
use pliron::location::{Located, Location};
use pliron::op::Op;
use pliron::operation::Operation;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    _destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0340"
        | "cuda_device::tma::cp_async_bulk_commit_group" => {
            Ok(Some(super::super::tma::emit_tma_commit_group(
                ctx, args, target, block_ptr, prev_op, block_map, loc, "v1:i0340",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0888"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_1d_l2" => {
            require_arity(name, args.len(), 2, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor1dL2Op::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0888");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_1d_l2 call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0917"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_1d_l2_cache_hint" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor1dL2CacheHintOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0917");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_1d_l2_cache_hint call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0889"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_2d_l2" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor2dL2Op::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0889");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_2d_l2 call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0918"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_2d_l2_cache_hint" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor2dL2CacheHintOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0918");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_2d_l2_cache_hint call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0890"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_3d_l2" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor3dL2Op::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0890");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_3d_l2 call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0919"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_3d_l2_cache_hint" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor3dL2CacheHintOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0919");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_3d_l2_cache_hint call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0891"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_4d_l2" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor4dL2Op::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0891");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_4d_l2 call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0920"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_4d_l2_cache_hint" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor4dL2CacheHintOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0920");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_4d_l2_cache_hint call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0892"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_5d_l2" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor5dL2Op::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0892");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_5d_l2 call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0921"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_5d_l2_cache_hint" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkPrefetchTensor5dL2CacheHintOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0921");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_5d_l2_cache_hint call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0893"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_gather4_2d_l2" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(ctx, dialect_nvvm::ops::CpAsyncBulkPrefetchTensorGather4TwoDimensionalL2Op::get_concrete_op_info(), vec![], operands, vec![], 0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0893");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_prefetch_tensor_gather4_2d_l2 call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0922"
        | "cuda_device::tma::cp_async_bulk_prefetch_tensor_gather4_2d_l2_cache_hint" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(ctx, dialect_nvvm::ops::CpAsyncBulkPrefetchTensorGather4TwoDimensionalL2CacheHintOp::get_concrete_op_info(), vec![], operands, vec![], 0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0922");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(loc, TranslationErr::unsupported("cp_async_bulk_prefetch_tensor_gather4_2d_l2_cache_hint call without target block".to_owned()))
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0328"
        | "cuda_device::tma::cp_async_bulk_tensor_1d_g2s" => {
            Ok(Some(super::super::tma::emit_tma_g2s(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 1,
                "v1:i0328",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0335"
        | "cuda_device::tma::cp_async_bulk_tensor_1d_s2g" => {
            Ok(Some(super::super::tma::emit_tma_s2g(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 1,
                "v1:i0335",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0329"
        | "cuda_device::tma::cp_async_bulk_tensor_2d_g2s" => {
            Ok(Some(super::super::tma::emit_tma_g2s(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 2,
                "v1:i0329",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0330"
        | "cuda_device::tma::cp_async_bulk_tensor_2d_g2s_multicast" => {
            Ok(Some(super::super::tma::emit_tma_g2s_multicast(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, "v1:i0330",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0331"
        | "cuda_device::tma::cp_async_bulk_tensor_2d_g2s_multicast_cg2" => {
            Ok(Some(super::super::tma::emit_tma_g2s_multicast_cg2(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, "v1:i0331",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0336"
        | "cuda_device::tma::cp_async_bulk_tensor_2d_s2g" => {
            Ok(Some(super::super::tma::emit_tma_s2g(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 2,
                "v1:i0336",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0332"
        | "cuda_device::tma::cp_async_bulk_tensor_3d_g2s" => {
            Ok(Some(super::super::tma::emit_tma_g2s(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 3,
                "v1:i0332",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0337"
        | "cuda_device::tma::cp_async_bulk_tensor_3d_s2g" => {
            Ok(Some(super::super::tma::emit_tma_s2g(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 3,
                "v1:i0337",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0333"
        | "cuda_device::tma::cp_async_bulk_tensor_4d_g2s" => {
            Ok(Some(super::super::tma::emit_tma_g2s(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 4,
                "v1:i0333",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0338"
        | "cuda_device::tma::cp_async_bulk_tensor_4d_s2g" => {
            Ok(Some(super::super::tma::emit_tma_s2g(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 4,
                "v1:i0338",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0334"
        | "cuda_device::tma::cp_async_bulk_tensor_5d_g2s" => {
            Ok(Some(super::super::tma::emit_tma_g2s(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 5,
                "v1:i0334",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0339"
        | "cuda_device::tma::cp_async_bulk_tensor_5d_s2g" => {
            Ok(Some(super::super::tma::emit_tma_s2g(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, 5,
                "v1:i0339",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0928"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_add_im2col_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAddIm2col3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0928");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_add_im2col_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0929"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_add_im2col_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAddIm2col4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0929");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_add_im2col_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0930"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_add_im2col_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAddIm2col5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0930");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_add_im2col_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0923"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_add_tile_1d" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAddTile1dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0923");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_add_tile_1d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0924"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_add_tile_2d" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAddTile2dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0924");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_add_tile_2d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0925"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_add_tile_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAddTile3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0925");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_add_tile_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0926"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_add_tile_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAddTile4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0926");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_add_tile_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0927"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_add_tile_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAddTile5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0927");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_add_tile_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0936"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_and_im2col_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAndIm2col3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0936");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_and_im2col_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0937"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_and_im2col_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAndIm2col4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0937");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_and_im2col_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0938"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_and_im2col_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAndIm2col5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0938");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_and_im2col_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0931"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_and_tile_1d" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAndTile1dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0931");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_and_tile_1d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0932"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_and_tile_2d" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAndTile2dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0932");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_and_tile_2d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0933"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_and_tile_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAndTile3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0933");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_and_tile_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0934"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_and_tile_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAndTile4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0934");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_and_tile_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0935"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_and_tile_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceAndTile5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0935");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_and_tile_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0944"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_dec_im2col_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceDecIm2col3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0944");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_dec_im2col_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0945"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_dec_im2col_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceDecIm2col4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0945");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_dec_im2col_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0946"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_dec_im2col_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceDecIm2col5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0946");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_dec_im2col_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0939"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_dec_tile_1d" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceDecTile1dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0939");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_dec_tile_1d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0940"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_dec_tile_2d" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceDecTile2dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0940");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_dec_tile_2d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0941"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_dec_tile_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceDecTile3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0941");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_dec_tile_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0942"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_dec_tile_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceDecTile4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0942");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_dec_tile_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0943"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_dec_tile_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceDecTile5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0943");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_dec_tile_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0952"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_inc_im2col_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceIncIm2col3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0952");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_inc_im2col_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0953"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_inc_im2col_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceIncIm2col4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0953");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_inc_im2col_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0954"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_inc_im2col_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceIncIm2col5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0954");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_inc_im2col_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0947"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_inc_tile_1d" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceIncTile1dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0947");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_inc_tile_1d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0948"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_inc_tile_2d" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceIncTile2dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0948");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_inc_tile_2d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0949"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_inc_tile_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceIncTile3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0949");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_inc_tile_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0950"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_inc_tile_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceIncTile4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0950");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_inc_tile_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0951"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_inc_tile_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceIncTile5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0951");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_inc_tile_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0960"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_max_im2col_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMaxIm2col3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0960");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_max_im2col_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0961"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_max_im2col_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMaxIm2col4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0961");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_max_im2col_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0962"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_max_im2col_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMaxIm2col5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0962");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_max_im2col_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0955"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_max_tile_1d" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMaxTile1dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0955");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_max_tile_1d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0956"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_max_tile_2d" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMaxTile2dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0956");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_max_tile_2d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0957"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_max_tile_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMaxTile3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0957");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_max_tile_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0958"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_max_tile_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMaxTile4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0958");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_max_tile_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0959"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_max_tile_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMaxTile5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0959");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_max_tile_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0968"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_min_im2col_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMinIm2col3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0968");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_min_im2col_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0969"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_min_im2col_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMinIm2col4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0969");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_min_im2col_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0970"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_min_im2col_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMinIm2col5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0970");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_min_im2col_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0963"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_min_tile_1d" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMinTile1dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0963");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_min_tile_1d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0964"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_min_tile_2d" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMinTile2dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0964");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_min_tile_2d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0965"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_min_tile_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMinTile3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0965");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_min_tile_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0966"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_min_tile_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMinTile4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0966");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_min_tile_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0967"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_min_tile_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceMinTile5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0967");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_min_tile_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0976"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_or_im2col_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceOrIm2col3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0976");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_or_im2col_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0977"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_or_im2col_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceOrIm2col4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0977");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_or_im2col_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0978"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_or_im2col_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceOrIm2col5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0978");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_or_im2col_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0971"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_or_tile_1d" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceOrTile1dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0971");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_or_tile_1d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0972"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_or_tile_2d" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceOrTile2dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0972");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_or_tile_2d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0973"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_or_tile_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceOrTile3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0973");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_or_tile_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0974"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_or_tile_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceOrTile4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0974");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_or_tile_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0975"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_or_tile_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceOrTile5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0975");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_or_tile_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0984"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_xor_im2col_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceXorIm2col3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0984");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_xor_im2col_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0985"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_xor_im2col_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceXorIm2col4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0985");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_xor_im2col_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0986"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_xor_im2col_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceXorIm2col5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0986");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_xor_im2col_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0979"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_xor_tile_1d" => {
            require_arity(name, args.len(), 3, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceXorTile1dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0979");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_xor_tile_1d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0980"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_xor_tile_2d" => {
            require_arity(name, args.len(), 4, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceXorTile2dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0980");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_xor_tile_2d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0981"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_xor_tile_3d" => {
            require_arity(name, args.len(), 5, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceXorTile3dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0981");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_xor_tile_3d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0982"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_xor_tile_4d" => {
            require_arity(name, args.len(), 6, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceXorTile4dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0982");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_xor_tile_4d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0983"
        | "cuda_device::tma::cp_async_bulk_tensor_reduce_xor_tile_5d" => {
            require_arity(name, args.len(), 7, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::CpAsyncBulkTensorReduceXorTile5dOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0983");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "cp_async_bulk_tensor_reduce_xor_tile_5d call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0341"
        | "cuda_device::tma::cp_async_bulk_wait_group" => {
            require_arity(name, args.len(), 1, &loc)?;
            if !matches!(args.first(), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "TMA wait-group count must be a compile-time constant".to_owned()
                    )
                );
            }
            Ok(Some(super::super::tma::emit_tma_wait_group(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, false,
                "v1:i0341",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0342"
        | "cuda_device::tma::cp_async_bulk_wait_group_read" => {
            require_arity(name, args.len(), 1, &loc)?;
            if !matches!(args.first(), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "TMA wait-group count must be a compile-time constant".to_owned()
                    )
                );
            }
            Ok(Some(super::super::tma::emit_tma_wait_group(
                ctx, body, args, target, block_ptr, prev_op, value_map, block_map, loc, true,
                "v1:i0342",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0905"
        | "cuda_device::tma::fence_proxy_tensormap_generic_acquire_cluster" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::FenceProxyTensorMapGenericAcquireClusterOp::get_concrete_op_info(
                ),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0905");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "fence_proxy_tensormap_generic_acquire_cluster call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0906"
        | "cuda_device::tma::fence_proxy_tensormap_generic_acquire_cta" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::FenceProxyTensorMapGenericAcquireCtaOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0906");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "fence_proxy_tensormap_generic_acquire_cta call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0907"
        | "cuda_device::tma::fence_proxy_tensormap_generic_acquire_gpu" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::FenceProxyTensorMapGenericAcquireGpuOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0907");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "fence_proxy_tensormap_generic_acquire_gpu call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0908"
        | "cuda_device::tma::fence_proxy_tensormap_generic_acquire_system" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::FenceProxyTensorMapGenericAcquireSystemOp::get_concrete_op_info(
                ),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0908");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "fence_proxy_tensormap_generic_acquire_system call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0909"
        | "cuda_device::tma::fence_proxy_tensormap_generic_release_cluster" => {
            require_arity(name, args.len(), 0, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::FenceProxyTensorMapGenericReleaseClusterOp::get_concrete_op_info(
                ),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0909");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "fence_proxy_tensormap_generic_release_cluster call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0910"
        | "cuda_device::tma::fence_proxy_tensormap_generic_release_cta" => {
            require_arity(name, args.len(), 0, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::FenceProxyTensorMapGenericReleaseCtaOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0910");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "fence_proxy_tensormap_generic_release_cta call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0911"
        | "cuda_device::tma::fence_proxy_tensormap_generic_release_gpu" => {
            require_arity(name, args.len(), 0, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::FenceProxyTensorMapGenericReleaseGpuOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0911");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "fence_proxy_tensormap_generic_release_gpu call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0912"
        | "cuda_device::tma::fence_proxy_tensormap_generic_release_system" => {
            require_arity(name, args.len(), 0, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::FenceProxyTensorMapGenericReleaseSystemOp::get_concrete_op_info(
                ),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0912");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "fence_proxy_tensormap_generic_release_system call without target block"
                            .to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0887"
        | "cuda_device::tma::prefetch_tma_descriptor" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::PrefetchTensorMapOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0887");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "prefetch_tma_descriptor call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0894"
        | "cuda_device::tma::tensormap_replace_box_dim" => {
            require_arity(name, args.len(), 3, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapBoxDimOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0894");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_box_dim call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0895"
        | "cuda_device::tma::tensormap_replace_element_stride" => {
            require_arity(name, args.len(), 3, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapElementStrideOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0895");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_element_stride call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0896"
        | "cuda_device::tma::tensormap_replace_element_type" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapElementTypeOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0896");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_element_type call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0897"
        | "cuda_device::tma::tensormap_replace_fill_mode" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapFillModeOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0897");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_fill_mode call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0898"
        | "cuda_device::tma::tensormap_replace_global_address" => {
            require_arity(name, args.len(), 2, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapGlobalAddressOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0898");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_global_address call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0899"
        | "cuda_device::tma::tensormap_replace_global_dim" => {
            require_arity(name, args.len(), 3, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapGlobalDimOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0899");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_global_dim call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0900"
        | "cuda_device::tma::tensormap_replace_global_stride" => {
            require_arity(name, args.len(), 3, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapGlobalStrideOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0900");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_global_stride call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0901"
        | "cuda_device::tma::tensormap_replace_interleave_layout" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapInterleaveLayoutOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0901");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_interleave_layout call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0902"
        | "cuda_device::tma::tensormap_replace_rank" => {
            require_arity(name, args.len(), 2, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapRankOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0902");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_rank call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0903"
        | "cuda_device::tma::tensormap_replace_swizzle_atomicity" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapSwizzleAtomicityOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0903");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_swizzle_atomicity call without target block".to_owned()
                    )
                )
            }
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i0904"
        | "cuda_device::tma::tensormap_replace_swizzle_mode" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensor-map replacement selector must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let intrinsic = Operation::new(
                ctx,
                dialect_nvvm::ops::ReplaceTensorMapSwizzleModeOp::get_concrete_op_info(),
                vec![],
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i0904");
            helpers::insert_op(ctx, intrinsic, block_ptr, last_op);
            if let Some(target_idx) = target {
                Ok(Some(helpers::emit_goto(
                    ctx,
                    *target_idx,
                    intrinsic,
                    block_map,
                    loc,
                )))
            } else {
                input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tensormap_replace_swizzle_mode call without target block".to_owned()
                    )
                )
            }
        }
        _ => Ok(None),
    }
}
