/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated counted-barrier, grid-dependency, and register-control operations.

use pliron::utils::apint::APInt;
use pliron::{
    builtin::{
        attributes::IntegerAttr,
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::{IntegerType, Signedness},
    },
    common_traits::Verify,
    context::{Context, Ptr},
    identifier::Identifier,
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    verify_err,
};
use pliron_derive::pliron_op;
use std::num::NonZeroUsize;

/// Signals arrival of the requested number of CTA threads at a numbered barrier without waiting.
#[pliron_op(
    name = "nvvm.barrier_cta_arrive_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<0>],
)]
pub struct BarrierCtaArriveCountOp;

impl BarrierCtaArriveCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Signals arrival of the requested number of CTA threads at an aligned numbered barrier without waiting.
#[pliron_op(
    name = "nvvm.barrier_cta_arrive_aligned_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<0>],
)]
pub struct BarrierCtaArriveAlignedCountOp;

impl BarrierCtaArriveAlignedCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Synchronizes the requested number of CTA threads at a numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_sync_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<0>],
)]
pub struct BarrierCtaSyncCountOp;

impl BarrierCtaSyncCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Synchronizes the requested number of CTA threads at an aligned numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_sync_aligned_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<0>],
)]
pub struct BarrierCtaSyncAlignedCountOp;

impl BarrierCtaSyncAlignedCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Makes dependent grids eligible to launch before the current grid completes.
#[pliron_op(
    name = "nvvm.grid_dependency_launch_dependents",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<0>, NResultsInterface<0>],
)]
pub struct GridDependencyLaunchDependentsOp;

impl GridDependencyLaunchDependentsOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Waits until all prerequisite grids have completed.
#[pliron_op(
    name = "nvvm.grid_dependency_wait",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<0>, NResultsInterface<0>],
)]
pub struct GridDependencyWaitOp;

impl GridDependencyWaitOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Decreases the executing warpgroup's maximum register allocation to the immediate count.
#[pliron_op(
    name = "nvvm.setmaxnreg_dec",
    format,
    interfaces = [NOpdsInterface<0>, NResultsInterface<0>],
)]
pub struct SetMaxNRegDecOp;

impl SetMaxNRegDecOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, register_count: u32) -> Ptr<Operation> {
        let op = Operation::new(ctx, Self::get_concrete_op_info(), vec![], vec![], vec![], 0);
        let ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        let value = APInt::from_u64(register_count.into(), NonZeroUsize::new(32).unwrap());
        op.deref_mut(ctx).attributes.set(
            Identifier::try_from("register_count").unwrap(),
            IntegerAttr::new(ty, value),
        );
        op
    }

    pub fn register_count(&self, ctx: &Context) -> Option<u32> {
        let key = Identifier::try_from("register_count").unwrap();
        let operation = self.get_operation().deref(ctx);
        let attribute: &IntegerAttr = operation.attributes.get(&key)?;
        let ty_handle = attribute.get_type();
        let ty = ty_handle.deref(ctx);
        if ty.width() != 32 || ty.signedness() != Signedness::Unsigned {
            return None;
        }
        u32::try_from(attribute.value().to_u64())
            .ok()
            .filter(|value| (24..=256).contains(value) && value % 8 == 0)
    }
}

impl Verify for SetMaxNRegDecOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if self.register_count(ctx).is_none() {
            return verify_err!(
                op.loc(),
                "setmaxnreg requires an immediate register count in 24..=256 divisible by 8"
            );
        }
        Ok(())
    }
}

/// Increases the executing warpgroup's maximum register allocation to the immediate count.
#[pliron_op(
    name = "nvvm.setmaxnreg_inc",
    format,
    interfaces = [NOpdsInterface<0>, NResultsInterface<0>],
)]
pub struct SetMaxNRegIncOp;

impl SetMaxNRegIncOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, register_count: u32) -> Ptr<Operation> {
        let op = Operation::new(ctx, Self::get_concrete_op_info(), vec![], vec![], vec![], 0);
        let ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        let value = APInt::from_u64(register_count.into(), NonZeroUsize::new(32).unwrap());
        op.deref_mut(ctx).attributes.set(
            Identifier::try_from("register_count").unwrap(),
            IntegerAttr::new(ty, value),
        );
        op
    }

    pub fn register_count(&self, ctx: &Context) -> Option<u32> {
        let key = Identifier::try_from("register_count").unwrap();
        let operation = self.get_operation().deref(ctx);
        let attribute: &IntegerAttr = operation.attributes.get(&key)?;
        let ty_handle = attribute.get_type();
        let ty = ty_handle.deref(ctx);
        if ty.width() != 32 || ty.signedness() != Signedness::Unsigned {
            return None;
        }
        u32::try_from(attribute.value().to_u64())
            .ok()
            .filter(|value| (24..=256).contains(value) && value % 8 == 0)
    }
}

impl Verify for SetMaxNRegIncOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if self.register_count(ctx).is_none() {
            return verify_err!(
                op.loc(),
                "setmaxnreg requires an immediate register count in 24..=256 divisible by 8"
            );
        }
        Ok(())
    }
}

pub(super) fn register(ctx: &mut Context) {
    BarrierCtaArriveCountOp::register(ctx);
    BarrierCtaArriveAlignedCountOp::register(ctx);
    BarrierCtaSyncCountOp::register(ctx);
    BarrierCtaSyncAlignedCountOp::register(ctx);
    GridDependencyLaunchDependentsOp::register(ctx);
    GridDependencyWaitOp::register(ctx);
    SetMaxNRegDecOp::register(ctx);
    SetMaxNRegIncOp::register(ctx);
}
