/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Structural operations for generated extended integer min/max.

use pliron::{
    builtin::{
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::{IntegerType, Signedness},
    },
    common_traits::Verify,
    context::{Context, Ptr},
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    r#type::Typed,
    value::Value,
    verify_err,
};
use pliron_derive::pliron_op;

fn is_i32(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == 32)
}

/// Computes the lane-wise signed maximum of two packed s16x2 values and clamps each lane to 0 if negative.
///
/// Lowers to `max.relu.s16x2`.
#[pliron_op(
    name = "nvvm.max_relu_s16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MaxReluS16x2Op;

impl MaxReluS16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MaxReluS16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.max_relu_s16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.max_relu_s16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes the signed 32-bit maximum and clamps the result to 0 if negative.
///
/// Lowers to `max.relu.s32`.
#[pliron_op(
    name = "nvvm.max_relu_s32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MaxReluS32Op;

impl MaxReluS32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Signed);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MaxReluS32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.max_relu_s32 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.max_relu_s32 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes the lane-wise signed maximum of two packed s16x2 values.
///
/// Lowers to `max.s16x2`.
#[pliron_op(
    name = "nvvm.max_s16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MaxS16x2Op;

impl MaxS16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MaxS16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.max_s16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.max_s16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes the lane-wise unsigned maximum of two packed u16x2 values.
///
/// Lowers to `max.u16x2`.
#[pliron_op(
    name = "nvvm.max_u16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MaxU16x2Op;

impl MaxU16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MaxU16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.max_u16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.max_u16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes the lane-wise signed minimum of two packed s16x2 values and clamps each lane to 0 if negative.
///
/// Lowers to `min.relu.s16x2`.
#[pliron_op(
    name = "nvvm.min_relu_s16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MinReluS16x2Op;

impl MinReluS16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MinReluS16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.min_relu_s16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.min_relu_s16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes the signed 32-bit minimum and clamps the result to 0 if negative.
///
/// Lowers to `min.relu.s32`.
#[pliron_op(
    name = "nvvm.min_relu_s32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MinReluS32Op;

impl MinReluS32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Signed);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MinReluS32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.min_relu_s32 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.min_relu_s32 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes the lane-wise signed minimum of two packed s16x2 values.
///
/// Lowers to `min.s16x2`.
#[pliron_op(
    name = "nvvm.min_s16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MinS16x2Op;

impl MinS16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MinS16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.min_s16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.min_s16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes the lane-wise unsigned minimum of two packed u16x2 values.
///
/// Lowers to `min.u16x2`.
#[pliron_op(
    name = "nvvm.min_u16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MinU16x2Op;

impl MinU16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MinU16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.min_u16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.min_u16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

pub(super) fn register(ctx: &mut Context) {
    MaxReluS16x2Op::register(ctx);
    MaxReluS32Op::register(ctx);
    MaxS16x2Op::register(ctx);
    MaxU16x2Op::register(ctx);
    MinReluS16x2Op::register(ctx);
    MinReluS32Op::register(ctx);
    MinS16x2Op::register(ctx);
    MinU16x2Op::register(ctx);
}
