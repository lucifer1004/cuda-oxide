/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Structural operations for generated packed floating-point arithmetic.

use pliron::{
    builtin::{
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::{IntegerType, Signedness},
    },
    common_traits::Verify,
    context::{Context, Ptr},
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    r#type::Typed,
    value::Value,
    verify_err,
};
use pliron_derive::pliron_op;

fn is_integer_width(ctx: &Context, ty: pliron::r#type::TypeHandle, width: u32) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == width)
}

/// Takes the absolute value of both packed bf16x2 lanes.
///
/// Lowers to `abs.bf16x2`.
#[pliron_op(
    name = "nvvm.abs_bf16x2",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct AbsBf16x2Op;

impl AbsBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0],
            vec![],
            0,
        )
    }
}

impl Verify for AbsBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.abs_bf16x2 requires exactly 1 operands and one result"
            );
        }
        if !(0..1).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.abs_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Takes the absolute value of both packed f16x2 lanes.
///
/// Lowers to `abs.f16x2`.
#[pliron_op(
    name = "nvvm.abs_f16x2",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct AbsF16x2Op;

impl AbsF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0],
            vec![],
            0,
        )
    }
}

impl Verify for AbsF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.abs_f16x2 requires exactly 1 operands and one result"
            );
        }
        if !(0..1).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.abs_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Adds two packed bf16x2 values.
///
/// Lowers to `add.rn.bf16x2`.
#[pliron_op(
    name = "nvvm.add_bf16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct AddBf16x2Op;

impl AddBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for AddBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.add_bf16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.add_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Adds two packed f16x2 values.
///
/// Lowers to `add.rn.f16x2`.
#[pliron_op(
    name = "nvvm.add_f16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct AddF16x2Op;

impl AddF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for AddF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.add_f16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.add_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Adds corresponding lanes of two packed f32x2 values.
///
/// Lowers to `add.rn.f32x2`.
#[pliron_op(
    name = "nvvm.add_f32x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct AddF32x2Op;

impl AddF32x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for AddF32x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.add_f32x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 64))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 64)
        {
            return verify_err!(
                op.loc(),
                "nvvm.add_f32x2 operands and result must be 64-bit integers"
            );
        }
        Ok(())
    }
}

/// Adds corresponding packed f32x2 lanes and flushes subnormal inputs and results to sign-preserving zero.
///
/// Lowers to `add.rn.ftz.f32x2`.
#[pliron_op(
    name = "nvvm.add_ftz_f32x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct AddFtzF32x2Op;

impl AddFtzF32x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for AddFtzF32x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.add_ftz_f32x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 64))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 64)
        {
            return verify_err!(
                op.loc(),
                "nvvm.add_ftz_f32x2 operands and result must be 64-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed bf16x2 fused multiply-add.
///
/// Lowers to `fma.rn.bf16x2`.
#[pliron_op(
    name = "nvvm.fma_bf16x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaBf16x2Op;

impl FmaBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_bf16x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed f16x2 fused multiply-add.
///
/// Lowers to `fma.rn.f16x2`.
#[pliron_op(
    name = "nvvm.fma_f16x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaF16x2Op;

impl FmaF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_f16x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a fused multiply-add in corresponding packed f32x2 lanes.
///
/// Lowers to `fma.rn.f32x2`.
#[pliron_op(
    name = "nvvm.fma_f32x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaF32x2Op;

impl FmaF32x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaF32x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_f32x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 64))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 64)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_f32x2 operands and result must be 64-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed f16x2 fused multiply-add, flushing subnormals to zero.
///
/// Lowers to `fma.rn.ftz.f16x2`.
#[pliron_op(
    name = "nvvm.fma_ftz_f16x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaFtzF16x2Op;

impl FmaFtzF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaFtzF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_ftz_f16x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_ftz_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed f32x2 fused multiply-add and flushes subnormal inputs and results to sign-preserving zero.
///
/// Lowers to `fma.rn.ftz.f32x2`.
#[pliron_op(
    name = "nvvm.fma_ftz_f32x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaFtzF32x2Op;

impl FmaFtzF32x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaFtzF32x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_ftz_f32x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 64))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 64)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_ftz_f32x2 operands and result must be 64-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed f16x2 fused multiply-add, flushing subnormals to zero and applying ReLU.
///
/// Lowers to `fma.rn.ftz.relu.f16x2`.
#[pliron_op(
    name = "nvvm.fma_ftz_relu_f16x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaFtzReluF16x2Op;

impl FmaFtzReluF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaFtzReluF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_ftz_relu_f16x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_ftz_relu_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed f16x2 fused multiply-add, flushing subnormals to zero and clamping the result to [0.0, 1.0].
///
/// Lowers to `fma.rn.ftz.sat.f16x2`.
#[pliron_op(
    name = "nvvm.fma_ftz_sat_f16x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaFtzSatF16x2Op;

impl FmaFtzSatF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaFtzSatF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_ftz_sat_f16x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_ftz_sat_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed bf16x2 fused multiply-add and applies ReLU.
///
/// Lowers to `fma.rn.relu.bf16x2`.
#[pliron_op(
    name = "nvvm.fma_relu_bf16x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaReluBf16x2Op;

impl FmaReluBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaReluBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_relu_bf16x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_relu_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed f16x2 fused multiply-add and applies ReLU.
///
/// Lowers to `fma.rn.relu.f16x2`.
#[pliron_op(
    name = "nvvm.fma_relu_f16x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaReluF16x2Op;

impl FmaReluF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaReluF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_relu_f16x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_relu_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Computes a packed f16x2 fused multiply-add and clamps the result to [0.0, 1.0].
///
/// Lowers to `fma.rn.sat.f16x2`.
#[pliron_op(
    name = "nvvm.fma_sat_f16x2",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct FmaSatF16x2Op;

impl FmaSatF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value, arg2: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1, arg2],
            vec![],
            0,
        )
    }
}

impl Verify for FmaSatF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.fma_sat_f16x2 requires exactly 3 operands and one result"
            );
        }
        if !(0..3).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.fma_sat_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Selects the maximum in each packed bf16x2 lane.
///
/// Lowers to `max.bf16x2`.
#[pliron_op(
    name = "nvvm.max_bf16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MaxBf16x2Op;

impl MaxBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MaxBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.max_bf16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.max_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Selects the maximum in each packed f16x2 lane.
///
/// Lowers to `max.f16x2`.
#[pliron_op(
    name = "nvvm.max_f16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MaxF16x2Op;

impl MaxF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MaxF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.max_f16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.max_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Selects the minimum in each packed bf16x2 lane.
///
/// Lowers to `min.bf16x2`.
#[pliron_op(
    name = "nvvm.min_bf16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MinBf16x2Op;

impl MinBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MinBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.min_bf16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.min_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Selects the minimum in each packed f16x2 lane.
///
/// Lowers to `min.f16x2`.
#[pliron_op(
    name = "nvvm.min_f16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MinF16x2Op;

impl MinF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MinF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.min_f16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.min_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Multiplies two packed bf16x2 values.
///
/// Lowers to `mul.rn.bf16x2`.
#[pliron_op(
    name = "nvvm.mul_bf16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MulBf16x2Op;

impl MulBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MulBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.mul_bf16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.mul_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Multiplies two packed f16x2 values.
///
/// Lowers to `mul.rn.f16x2`.
#[pliron_op(
    name = "nvvm.mul_f16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MulF16x2Op;

impl MulF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MulF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.mul_f16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.mul_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Multiplies corresponding lanes of two packed f32x2 values.
///
/// Lowers to `mul.rn.f32x2`.
#[pliron_op(
    name = "nvvm.mul_f32x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MulF32x2Op;

impl MulF32x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MulF32x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.mul_f32x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 64))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 64)
        {
            return verify_err!(
                op.loc(),
                "nvvm.mul_f32x2 operands and result must be 64-bit integers"
            );
        }
        Ok(())
    }
}

/// Multiplies corresponding packed f32x2 lanes and flushes subnormal inputs and results to sign-preserving zero.
///
/// Lowers to `mul.rn.ftz.f32x2`.
#[pliron_op(
    name = "nvvm.mul_ftz_f32x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct MulFtzF32x2Op;

impl MulFtzF32x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for MulFtzF32x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.mul_ftz_f32x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 64))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 64)
        {
            return verify_err!(
                op.loc(),
                "nvvm.mul_ftz_f32x2 operands and result must be 64-bit integers"
            );
        }
        Ok(())
    }
}

/// Negates both packed bf16x2 lanes.
///
/// Lowers to `neg.bf16x2`.
#[pliron_op(
    name = "nvvm.neg_bf16x2",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct NegBf16x2Op;

impl NegBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0],
            vec![],
            0,
        )
    }
}

impl Verify for NegBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.neg_bf16x2 requires exactly 1 operands and one result"
            );
        }
        if !(0..1).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.neg_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Negates both packed f16x2 lanes.
///
/// Lowers to `neg.f16x2`.
#[pliron_op(
    name = "nvvm.neg_f16x2",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct NegF16x2Op;

impl NegF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0],
            vec![],
            0,
        )
    }
}

impl Verify for NegF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.neg_f16x2 requires exactly 1 operands and one result"
            );
        }
        if !(0..1).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.neg_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Subtracts two packed bf16x2 values.
///
/// Lowers to `sub.rn.bf16x2`.
#[pliron_op(
    name = "nvvm.sub_bf16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct SubBf16x2Op;

impl SubBf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for SubBf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.sub_bf16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.sub_bf16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Subtracts two packed f16x2 values.
///
/// Lowers to `sub.rn.f16x2`.
#[pliron_op(
    name = "nvvm.sub_f16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct SubF16x2Op;

impl SubF16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for SubF16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.sub_f16x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 32))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.sub_f16x2 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Subtracts corresponding lanes of two packed f32x2 values.
///
/// Lowers to `sub.rn.f32x2`.
#[pliron_op(
    name = "nvvm.sub_f32x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct SubF32x2Op;

impl SubF32x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for SubF32x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.sub_f32x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 64))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 64)
        {
            return verify_err!(
                op.loc(),
                "nvvm.sub_f32x2 operands and result must be 64-bit integers"
            );
        }
        Ok(())
    }
}

/// Subtracts corresponding packed f32x2 lanes and flushes subnormal inputs and results to sign-preserving zero.
///
/// Lowers to `sub.rn.ftz.f32x2`.
#[pliron_op(
    name = "nvvm.sub_ftz_f32x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct SubFtzF32x2Op;

impl SubFtzF32x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, arg0: Value, arg1: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![arg0, arg1],
            vec![],
            0,
        )
    }
}

impl Verify for SubFtzF32x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.sub_ftz_f32x2 requires exactly 2 operands and one result"
            );
        }
        if !(0..2).all(|index| is_integer_width(ctx, op.get_operand(index).get_type(ctx), 64))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 64)
        {
            return verify_err!(
                op.loc(),
                "nvvm.sub_ftz_f32x2 operands and result must be 64-bit integers"
            );
        }
        Ok(())
    }
}

pub(super) fn register(ctx: &mut Context) {
    AbsBf16x2Op::register(ctx);
    AbsF16x2Op::register(ctx);
    AddBf16x2Op::register(ctx);
    AddF16x2Op::register(ctx);
    AddF32x2Op::register(ctx);
    AddFtzF32x2Op::register(ctx);
    FmaBf16x2Op::register(ctx);
    FmaF16x2Op::register(ctx);
    FmaF32x2Op::register(ctx);
    FmaFtzF16x2Op::register(ctx);
    FmaFtzF32x2Op::register(ctx);
    FmaFtzReluF16x2Op::register(ctx);
    FmaFtzSatF16x2Op::register(ctx);
    FmaReluBf16x2Op::register(ctx);
    FmaReluF16x2Op::register(ctx);
    FmaSatF16x2Op::register(ctx);
    MaxBf16x2Op::register(ctx);
    MaxF16x2Op::register(ctx);
    MinBf16x2Op::register(ctx);
    MinF16x2Op::register(ctx);
    MulBf16x2Op::register(ctx);
    MulF16x2Op::register(ctx);
    MulF32x2Op::register(ctx);
    MulFtzF32x2Op::register(ctx);
    NegBf16x2Op::register(ctx);
    NegF16x2Op::register(ctx);
    SubBf16x2Op::register(ctx);
    SubF16x2Op::register(ctx);
    SubF32x2Op::register(ctx);
    SubFtzF32x2Op::register(ctx);
}
