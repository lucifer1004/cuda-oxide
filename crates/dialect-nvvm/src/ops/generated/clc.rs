/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 3df52944985ca17502f6d46dac02c640ef6a6abb7784a1f72fb0dced7c24e3ad.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated Cluster Launch Control operations.

use pliron::{
    builtin::{
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::IntegerType,
    },
    common_traits::Verify,
    context::{Context, Ptr},
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    r#type::{TypeHandle, Typed},
    verify_err,
};
use pliron_derive::pliron_op;

/// Returns the X coordinate from a successful cancellation response.
#[pliron_op(
    name = "nvvm.clc_query_get_first_ctaid_x",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ClcQueryGetFirstCtaidXOp;

impl ClcQueryGetFirstCtaidXOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

impl Verify for ClcQueryGetFirstCtaidXOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        verify_query_shape(
            ctx,
            self.get_operation(),
            "nvvm.clc_query_get_first_ctaid_x",
        )
    }
}

/// Returns the Y coordinate from a successful cancellation response.
#[pliron_op(
    name = "nvvm.clc_query_get_first_ctaid_y",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ClcQueryGetFirstCtaidYOp;

impl ClcQueryGetFirstCtaidYOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

impl Verify for ClcQueryGetFirstCtaidYOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        verify_query_shape(
            ctx,
            self.get_operation(),
            "nvvm.clc_query_get_first_ctaid_y",
        )
    }
}

/// Returns the Z coordinate from a successful cancellation response.
#[pliron_op(
    name = "nvvm.clc_query_get_first_ctaid_z",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ClcQueryGetFirstCtaidZOp;

impl ClcQueryGetFirstCtaidZOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

impl Verify for ClcQueryGetFirstCtaidZOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        verify_query_shape(
            ctx,
            self.get_operation(),
            "nvvm.clc_query_get_first_ctaid_z",
        )
    }
}

/// Returns whether the Cluster Launch Control request was canceled.
#[pliron_op(
    name = "nvvm.clc_query_is_canceled",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ClcQueryIsCanceledOp;

impl ClcQueryIsCanceledOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

impl Verify for ClcQueryIsCanceledOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        verify_query_shape(ctx, self.get_operation(), "nvvm.clc_query_is_canceled")
    }
}

/// Requests one pending CTA and writes its response to shared memory.
#[pliron_op(
    name = "nvvm.clc_try_cancel",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<0>],
)]
pub struct ClcTryCancelOp;

impl ClcTryCancelOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Requests one pending CTA and multicasts its response across the cluster.
#[pliron_op(
    name = "nvvm.clc_try_cancel_multicast",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<0>],
)]
pub struct ClcTryCancelMulticastOp;

impl ClcTryCancelMulticastOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

fn verify_query_shape(ctx: &Context, operation: Ptr<Operation>, name: &str) -> Result<(), Error> {
    let op = operation.deref(ctx);
    let valid = op.get_num_operands() == 2
        && op.get_num_results() == 1
        && is_integer_width(ctx, op.get_operand(0).get_type(ctx), 64)
        && is_integer_width(ctx, op.get_operand(1).get_type(ctx), 64)
        && is_integer_width(ctx, op.get_result(0).get_type(ctx), 32);
    if !valid {
        return verify_err!(
            op.loc(),
            "{} requires two i64 operands and one i32 result",
            name
        );
    }
    Ok(())
}

fn is_integer_width(ctx: &Context, ty: TypeHandle, width: u32) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == width)
}

pub(super) fn register(ctx: &mut Context) {
    ClcQueryGetFirstCtaidXOp::register(ctx);
    ClcQueryGetFirstCtaidYOp::register(ctx);
    ClcQueryGetFirstCtaidZOp::register(ctx);
    ClcQueryIsCanceledOp::register(ctx);
    ClcTryCancelOp::register(ctx);
    ClcTryCancelMulticastOp::register(ctx);
}
